# Copy to .env and fill in values
PORT=8080
DATABASE_URL=sqlite://./data/ba-ide.db
# Generate with: openssl rand -hex 32
JWT_SECRET=change-me-with-a-real-random-secret
# Path to Next.js static export (relative to backend/)
STATIC_DIR=../out
